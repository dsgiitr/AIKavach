import torch
import torch.nn as nn
import time
import cv2

class denoiser(nn.Module):
    def __init__(self) -> None:
        super().__init__()
            self.very_small_denoiser = 
            self.small_denoiser = 
            self.medium_denoiser = 
            self.large_denoiser = 